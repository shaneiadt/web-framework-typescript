# Web Framework w/ TypeScript

Bare bones of a web framework similar to React / VueJs built using TypeScript & bundled using Parcel Bundler.

### Local Usage

Local server is json-server :smile:

```
npm start
```