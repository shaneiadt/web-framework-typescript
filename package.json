{
  "browserslist": ["Chrome 78"],
  "scripts": {
    "start:dev:db": "json-server -w db.json",
    "start:dev:parcel": "parcel index.html",
    "start": "concurrently npm:start:dev:*"
  },
  "devDependencies": {
    "typescript": "^3.7.3"
  },
  "dependencies": {
    "axios": "^0.21.2",
    "concurrently": "^8.2.0"
  }
}