import { User } from './models/User';

const user = new User({ name: 'New', age: 0 });

// user.save();
// user.fetch();

// setTimeout(() => {
//   user.set({
//     name: 'Shanesss'
//   });
//   user.save();
// }, 2000);